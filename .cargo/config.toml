# [unstable]
# codegen-backend = true
# [profile.server-dev]
# codegen-backend = "cranelift"

[alias]
sync-display-ftl = "run -p xtask -- sync-display-ftl"
sync-display-ftl-check = "run -p xtask -- sync-display-ftl --check"

[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = [ "-C", "link-arg=-fuse-ld=mold" ]
