{
  "name": "koruma",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "build": "turbo build",
    "build:wasm": "bun run build:wasm:collection",
    "build:wasm:collection": "CARGO_PROFILE_RELEASE_CODEGEN_UNITS=1 cargo build --release --target wasm32-unknown-unknown -p koruma-collection-example --no-default-features --features web && ~/.cargo/bin/wasm-bindgen --out-dir web/public/collection-example --target web target/wasm32-unknown-unknown/release/koruma-collection-example.wasm",
    "build:web": "turbo run build --filter=web",
    "dev": "turbo dev",
    "fmt": "oxfmt",
    "lint": "oxlint"
  },
  "devDependencies": {
    "oxfmt": "^0.27.0",
    "oxlint": "^1.42.0",
    "turbo": "^2"
  },
  "packageManager": "bun@1.3.5"
}
