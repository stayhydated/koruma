{
  "name": "koruma",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "build": "turbo build",
    "build:wasm": "bun run build:wasm:collection",
    "build:wasm:collection": "cd examples/collection-web && CARGO_TARGET_DIR=../../target/wasm wasm-pack build --release --target web --out-dir pkg && cp pkg/*.js pkg/*.wasm ../../web/public/collection-example/",
    "build:web": "turbo run build --filter=web",
    "dev": "turbo dev",
    "fmt": "oxfmt",
    "lint": "oxlint"
  },
  "devDependencies": {
    "oxfmt": "^0.31.0",
    "oxlint": "^1.46.0",
    "turbo": "^2.8.7"
  },
  "packageManager": "bun@1.3.5"
}
